{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sm</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sM.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ sM.contenu }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sM.titre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sms_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('sms_edit', { 'id': sM.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
